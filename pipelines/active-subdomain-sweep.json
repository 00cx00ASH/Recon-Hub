{
  "name": "active-subdomain-sweep",
  "description": "Enumeração ATIVA de subdomínio (wordlist + resolução DNS de verdade, com detecção e filtro de wildcard) e, em cima dos subdomínios achados, varredura de subdomain takeover. Complementa passive-takeover/crtsh-takeover — acha o que nunca apareceu num Certificate Transparency log.",
  "steps": [
    { "tool": "recon-subdomain-brute", "params": { "wordlist": "builtin/subdomains-common" } },
    {
      "tool": "scan-subdomain-takeover",
      "params": { "concurrency": 25, "only_confirmed": false },
      "feed": { "param": "subdomains", "source": "assets", "kind": "subdomain", "as": "csv" }
    }
  ]
}
